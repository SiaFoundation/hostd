name: Sync OpenAPI Versions

permissions:
  contents: write
  pull-requests: write

on:
  release:
    types: [published, edited]
  workflow_dispatch:

jobs:
  sync:
    uses: SiaFoundation/workflows/.github/workflows/sync-openapi-version.yml@3d496e2845d325cc657cedfa8ec7aba60953a1c3 # master
    with:
      spec_path: openapi.yml
